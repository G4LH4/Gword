import styled from "styled-components";

const StyledOutputA = styled.a`
  padding: 10px;
  font-family: "Open Sans", sans-serif;
  color: #ffd866;
  background-color: #000000;
  text-transform: uppercase;
  border-radius: 5px;
`;

export default StyledOutputA;
